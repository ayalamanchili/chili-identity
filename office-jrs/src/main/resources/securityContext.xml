<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:security="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    
    <!-- Security url configuration -->
    <http auto-config="true" create-session="stateless" use-expressions="true" entry-point-ref="digestEntryPoint">
        <intercept-url pattern="/*/file/*" access="permitAll" />
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
        <custom-filter ref="digestFilter" after="BASIC_AUTH_FILTER" />
    </http>
    <!-- Digest Auth Filter -->
    <beans:bean id="digestFilter" class="org.springframework.security.web.authentication.www.DigestAuthenticationFilter">
        <beans:property name="userDetailsService" ref="userService" />
        <beans:property name="authenticationEntryPoint" ref="digestEntryPoint" />
    </beans:bean>
    <!--Digest Entry point -->
    <beans:bean id="digestEntryPoint" class="org.springframework.security.web.authentication.www.DigestAuthenticationEntryPoint">
        <beans:property name="realmName" value="Portal Authentication"/>
        <beans:property name="key" value="acegi" />
    </beans:bean>
    
    <!-- Enable roles check annotations-->
    <security:global-method-security secured-annotations="enabled" pre-post-annotations="enabled" />
    <authentication-manager>
        <authentication-provider>
            <!-- password encodeer is conflicting with digest auth filter so disabling it until the issue is resolved http://yalamanchili.info:8080/jira/browse/SSTO-473-->
            <!--            <password-encoder ref="passwordEncoder" />-->
            <jdbc-user-service  id="userService" data-source-ref="officeSecurityDataSource"
                                users-by-username-query="select username, passwordHash ,enabled from CUser where username=?"

                                authorities-by-username-query="select user.username, role.rolename from cuser user, crole role, userroles userroles 
		      where user.userId = userroles.userId and userroles.roleId= role.roleId and user.username =?  " />
        </authentication-provider>
    </authentication-manager>
    <beans:bean id ="passwordEncoder" class="org.springframework.security.authentication.encoding.ShaPasswordEncoder" >
        <beans:constructor-arg value="256"/>
    </beans:bean>
</beans:beans>